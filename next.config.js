/** 
 * @type {import('next').NextConfig} 
 */
const nextConfig = {
    reactStrictMode: true,
    // basePath: '/website-template',
    output: 'export',
    trailingSlash: true,
    // images: {
    //     unoptimized: true
    // }
}

module.exports = nextConfig
